# STS-Upload client tool (Issue #15, Slice 2) runtime dependencies.
#
# The headless core (api client, card scanner, upload engine, resume
# state) needs only `requests`. The GUI adds PySide6. Hardware-monitoring
# and packaging deps come with their later Slice-2 blocks, kept out so the
# core stays importable/unit-testable without a display:
#   - pyudev (Linux) / wmi (Windows) -> mount/unmount monitoring block
#   - pyinstaller  -> distribution block
requests>=2.31
PySide6>=6.6
